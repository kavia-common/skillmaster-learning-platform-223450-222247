# Backend environment example

# MongoDB for catalog content (already used by the project)
# MONGODB_URI=mongodb://<user>:<pass>@<host>:<port>/<db>?retryWrites=true&w=majority

# JWT secret for admin routes (catalog mutations)
# JWT_SECRET=change-this-later

# Optional: Relational DB connection for Subject→Module→Lesson→Activity/Quiz & Progress
# If not set, defaults to local SQLite file ./skillmaster.db
# Examples:
#   SQLite: sqlite:///./skillmaster.db
#   Postgres: postgresql+psycopg://user:pass@host:5432/skillmaster
#   MySQL: mysql+pymysql://user:pass@host:3306/skillmaster
# SQLALCHEMY_DATABASE_URL=sqlite:///./skillmaster.db
