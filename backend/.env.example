# Backend environment example (do not commit real secrets)
# Relational DB (defaults to SQLite if unset)
SQLALCHEMY_DATABASE_URL=sqlite:///./skillmaster.db
# Mongo (optional for content endpoints)
MONGODB_URI=mongodb://localhost:27017/skillmaster
# JWT secret for admin-protected content routes
JWT_SECRET=change-this
# Allow frontend URL (CORS)
FRONTEND_URL=http://localhost:3000
# Enable auto seed on startup if desired
SEED_RELATIONAL_DATA=false
# Log level
LOG_LEVEL=INFO
