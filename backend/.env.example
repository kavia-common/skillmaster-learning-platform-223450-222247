# Backend environment variables

# Relational DB URL. Defaults to SQLite file if not set.
# SQLALCHEMY_DATABASE_URL=sqlite:///./skillmaster.db
# Example Postgres:
# SQLALCHEMY_DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# MongoDB (optional for content catalog)
# MONGODB_URI=mongodb://user:pass@localhost:27017/skillmaster?retryWrites=true&w=majority

# JWT secret for admin operations (development only value)
# JWT_SECRET=change-this-later

# Seed relational Subjects → Skills → Modules → Lessons → Activities on startup
SEED_RELATIONAL_DATA=true

# Configure frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Service host/port (when running uvicorn manually)
# HOST=0.0.0.0
# PORT=3001
