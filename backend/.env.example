# Backend environment example for SkillMaster
# Copy to .env and fill values as appropriate for your environment.

# FastAPI
APP_ENV=development
APP_PORT=8000

# JWT
JWT_SECRET=change-this-later

# MongoDB (existing components rely on this)
MONGODB_URI=mongodb://localhost:27017
MONGO_DB=skillmaster

# Relational DB URL for SQLAlchemy (sync engine used by src/db/sqlalchemy.py)
# Choose one:
# SQLite (recommended for local/dev; stdlib sqlite3 driver)
# Note: check_same_thread=False is handled automatically by the engine setup for SQLite.
SQLALCHEMY_DATABASE_URL=sqlite:///./skillmaster.db

# PostgreSQL (sync, psycopg2-binary)
# SQLALCHEMY_DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# Seeding relational data on startup
# SEED_RELATIONAL_DATA=true
